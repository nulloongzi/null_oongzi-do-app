# 기능 패리티 매트릭스 — 웹앱 ↔ Flutter 앱

> 목표: **한쪽에만 구현된 기능 0개** (플랫폼 특성상 다른 것 제외).
> 기준: 웹 `null_oongzi-do`(main) ↔ 앱 `null_oongzi-do-app`(`claude/gifted-hamilton-UaSAK` @ `28db107`).
> 갱신일: 2026-07-08. 양쪽 코드 전수 조사 + 갈림 지점 코드 검증 완료.

## 1. 매트릭스 (도메인별)

범례: ✅ 양쪽 | 🌐 웹만 | 📱 앱만 | 🔧 플랫폼 특성(이식 불필요)

### 지도/마커
| 기능 | 웹 | 앱 |
|---|---|---|
| 마커(노랑/빨강) + 클러스터링 + 급구 상시 노출 | ✅ | ✅ |
| 줌 연동 클럽명 라벨(인증 ✓·급구 🔥) | ✅ | ✅ |
| 라벨 페이드 인 + 줌 히스테리시스 | 🌐 즉시 토글 | 📱 |
| 라벨 겹침 회피(club.angle 앵커) | 🌐 | — |
| 마커 탭 → 카메라 포커스 + 상세 | ✅ | ✅ |
| 급구 롤링 티커(탭→상세) | ✅ 3초 세로 | ✅ 4초 슬라이드 |
| 내 위치 FAB | ✅ | ✅ |
| 필터/검색 결과 fitBounds | ✅ | ✅ |
| **마커 롱프레스 → 블러 + 릴스 피크** | — | 📱 |

### 검색/필터
| 기능 | 웹 | 앱 |
|---|---|---|
| 실시간 검색(동호회/픽업별 대상 필드) | ✅ | ✅ |
| 탭별 검색 placeholder 교체 | 🌐 | — |
| 필터 시트(지역8·요일7·대상7, 확장 매칭, 적용/초기화) | ✅ | ✅ |
| EN 모드 6인제 강조 + 힌트 박스 | ✅ | ✅ |
| 필터 활성 빨간 점 배지 | ✅ | ✅ |
| 필터시트 안 키워드 입력(검색바 동기화) | — | 📱 |
| 픽업 English-OK 토글 | ✅ | ✅ |
| 픽업 지역 필터 (region 필드 우선 · 주소 접두어 폴백) | ✅ | ✅ |
| 픽업 레벨 필터 (`any` 크루는 항상 포함) | ✅ | ✅ |
| 레벨 4단계 + 언어별 라벨 (KO 입문~선출 / EN B~AA·Open) | ✅ | ✅ |
| 레벨 설명 + 자가선택 가이드 (폼·필터 양쪽) | ✅ | ✅ |
| 픽업 목록 공유 링크 (`?tab=pickup&region=&level=&english=1`) | ✅ 착지 | ✅ 웹 URL 생성 |
| 키보드 포커스 관리(onTapOutside 등) | 🔧 | 📱 |

### 클럽 상세
| 기능 | 웹 | 앱 |
|---|---|---|
| peek↔expand 드래그 + 요약↔시간표 morph + 오늘 강조 | ✅ | ✅ |
| 태그·회비·주소 복사·길찾기·공유·북마크 | ✅ | ✅ |
| 인스타 릴스 임베드 | ✅ 단일 | ✅ **멀티**(캐러셀/더보기) |
| 릴스 지연 로딩(포스터→탭재생) | — | 📱 |
| 급구 배너 + 올리기/내리기 | ✅ **인증팀만** | ✅ 모든 소유자 ⚠️정책차 |
| 인증 신청(사진 업로드) | ✅ | ✅ |
| **인증 상태 표시(심사 중/거절 사유/재신청)** | 🌐 | — |
| 수정/삭제(소유자·관리자) | ✅ | ✅ |
| 언어 전환 시 열린 시트 재렌더 | ✅ | ⚠️ 검증 필요 |
| 픽업 상세: 비고(notes)·일정 메모 행 | 🌐 | — ⚠️ 폼은 저장하는데 미표시 |

### 등록/수정
| 기능 | 웹 | 앱 |
|---|---|---|
| 클럽 폼(필드·검증·길이 가드·수정 프리필) | ✅ | ✅ |
| 스케줄 블록 에디터 | ✅ | ✅ |
| 지도 피커 + 지오코딩 | ✅ 카카오 SDK | ✅ Cloud Fn |
| 주소 직접 수정 시 좌표 리셋 | 🌐 | — |
| **릴스 다중 입력 + 드래그 정렬** | — 단일 입력 | 📱 |
| **관리자 소유자 재지정 필드** | 🌐 | — |
| 픽업 폼(전 필드·만료 프리셋·익명 등록) | ✅ | ✅ |
| 픽업 좌표·주소 선택 입력(좌표 없으면 목록 전용) | ✅ | ✅ |
| 픽업 인스타 핸들 필드 + 목록/상세 노출 | ✅ | ✅ |
| 관리자 전용 '대신 등록' 토글 (`source='curated'`) | ✅ | ✅ |
| 좌표 없는 크루 안내(목록 뷰 유도) | — 목록 상시 노출 | 📱 |

### 도시락 / 프로필
| 기능 | 웹 | 앱 |
|---|---|---|
| 중앙 팝업(블러 딤+스프링), 벤토 그리드, 편집/스왑/✕, 커스텀 팀, 셀→상세, 식단표 아코디언 | ✅ | ✅ |
| **비로그인 localStorage 폴백 + 로그인 시 병합** | 🌐 | — (앱은 로그인 강제) |
| 도시락 저장 방식 | merge 쓰기 | 📱 트랜잭션+롤백(더 견고) |
| 프로필 카드(밥이름·워터마크·대표팀·🥢개명·포장하기) | ✅ | ✅ |
| 로그아웃 confirm | 🌐 | — |
| 밥이름 중복 재시도(10회) + 환영 인사 | 🌐 | ⚠️ 검증 필요 |
| **비로그인 둘러보기(게스트 모드)** | 🌐 전체 열람 가능 | — AuthGate가 로그인 강제 |

### 공유
| 기능 | 웹 | 앱 |
|---|---|---|
| 공유 메뉴(스토리/링크 복사/OS 시트) | ✅ | ✅ |
| **카카오톡 공유(리치카드→폴백 체인)** | 🌐 | — (v1 의도 보류: SDK+콘솔 필요) |
| 스토리 카드(1080×1920, 지하철역, QR) | ✅ canvas | ✅ dart:ui |
| 스토리 코치 모달(1회) + 링크 자동 복사 | ✅ | ✅ |
| 인스타 스토리 네이티브 인텐트 | ✅ 셸 브리지 | 📱 직접 |
| 포장하기 캡처 | ✅ **피드형/스토리형 2모드** + 미리보기/저장 | ✅ 단일 모드 → OS 시트 |
| 딥링크(?club/?spot) 수신 | ✅ URL | ✅ App Links |
| URL 주소창 동기화 / OG 메타 / PWA | 🔧 웹 특성 | — |

### 기타
| 기능 | 웹 | 앱 |
|---|---|---|
| KO/EN 토글 + 데이터 표시 변환 | ✅ | ✅ |
| 애널리틱스 | ✅ `club_contact` 有 | ✅ `reel_peek`·`remove_bookmark` 有 |
| 뒤로가기 계층 + 2번 종료, 권한, BounceTap/Glass | 🔧 | 📱 앱 특성 |
| 관리자 카카오 챗봇·인증 승인 링크·재지정 Fn | 공용 백엔드(양쪽 서비스) | ← |

## 2. 패리티 백로그

### 웹 → 앱 (앱에 넣을 것)
| # | 항목 | 크기 | 비고 |
|---|---|---|---|
| A1 | **비로그인 둘러보기(게스트 모드)** — 열람은 무로그인, 등록/찜만 로그인 유도 | L | 구조 변경(AuthGate). 제품 결정 필요 |
| A2 | **인증 신청 상태 표시** — 심사 중/거절 사유/재신청 버튼 | M | verification_requests 최신 조회 |
| A3 | 카카오톡 공유 | M~L | Kakao 콘솔/SDK 선행(사람 작업) |
| A4 | 관리자 소유자 재지정 폼 필드 | M | adminReassignOwner 호출 |
| A5 | 픽업 상세에 비고(notes)·일정 메모 표시 | S | **사실상 버그** — 저장만 되고 안 보임 |
| A6 | 포장하기 피드형/스토리형 2모드 | S~M | |
| A7 | 탭별 검색 placeholder | S | |
| A8 | club_contact 애널리틱스(insta/link/directions) | S | |
| A9 | 주소 수정 시 좌표 리셋 | S | stale 좌표 방지 |
| A10 | 급구 토글 인증팀 제한 | S | ⚠️ 정책 결정: 웹 기준 통일? |
| A11 | 로그아웃 confirm | S | |
| A12 | 라벨 겹침 각도(club.angle) | S | 데이터 활용 |

### 앱 → 웹 (웹에 넣을 것)
| # | 항목 | 크기 | 비고 |
|---|---|---|---|
| W1 | **멀티 릴스** — 폼 다중 입력(+정렬), 상세 캐러셀/더보기 | M | ⚠️ 데이터 비대칭: 앱 등록분의 `insta_reels`를 웹이 첫 개만 표시 중 |
| W2 | 릴스 미리보기 제스처 — 마커/라벨 롱프레스(터치)·hover(데스크톱) 피크 | M | |
| W3 | 릴스 지연 로딩(포스터→탭재생) | S~M | 성능 개선 겸 |
| W4 | remove_bookmark 애널리틱스 | S | |
| W5 | 라벨 등장 페이드(CSS transition) | S | 폴리시 |
| W6 | 필터시트 내 키워드 필드 | S | 선택 |

### 진행 현황 (2026-07-08)
- ✅ 완료(앱): A1 게스트 모드 · A2 인증 상태 표시 · A4 소유자 재지정 · A5 픽업 비고/일정메모 표시 · A6 포장하기 2모드 · A7 탭별 placeholder · A8 club_contact 추적 · A9 좌표 리셋 · A10 급구 인증팀 제한(웹 기준 통일) · A11 로그아웃 confirm
- ✅ 완료: A3 카카오톡 공유 — kakao_flutter_sdk_share **리치카드**(제목+로고+'지도에서 보기' 버튼, 웹 Kakao.Share 대응). 미설치/실패는 OS 시트 폴백.
  - 콘솔 등록값: 패키지명 `com.nulloongzi.nulloongzido` · **키 해시 `7+iGjU4VLiV7d68cUCIRKlmtUHA=`** (debug-fixed.keystore 기준 — CI/로컬 동일). 릴리즈 키스토어 도입 시 그 키 해시도 추가 등록 필요.
- 🚫 제외: A12 라벨 겹침 각도 — 앱은 라벨+핀이 한 장의 합성 이미지라 웹처럼 라벨만 xAnchor 이동 불가(구조적). 필요 시 라벨을 Flutter 오버레이로 분리하는 별도 작업으로.
- ✅ 완료(웹, `claude/keen-cannon-93mnA`): W1 멀티 릴스(폼 textarea·insta_reels 저장·상세 더보기) · W2 라벨 롱프레스 릴스 피크(릴스 없는 팀은 무시 — 앱은 스낵바) · W3 릴스 지연 로딩(포스터→탭재생) · W4 remove_bookmark 추적 · W5 라벨 등장 페이드 · W6 필터시트 키워드
- 🏁 결론: 패리티 백로그 소진. 잔여는 A3(카카오 콘솔 대기)와 A12(구조적 제외)뿐.

### 진행 현황 (2026-07-27)
- ✅ 완료(앱): 카카오/네이버 소셜 로그인 포팅 (웹 #41 대응)
  - 카카오: `kakao_flutter_sdk_user` 톡 앱 우선 → 계정 폴백 → CF `kakaoCustomToken({accessToken})` → 커스텀 토큰. 웹과 동일 계정(`kakao:{id}`)으로 이어짐.
  - 네이버: `flutter_naver_login` 구조 완성. **client secret은 커밋 금지**(이 저장소는 공개) — 빌드 시점 주입 구조를 쓴다:
    - 로컬: `cp android/secrets.properties.example android/secrets.properties` 후 `naverClientSecret=` 채우기 (iOS는 `ios/Flutter/Secrets.xcconfig`). 둘 다 gitignore.
    - CI: 리포지토리 시크릿 `NAVER_CLIENT_SECRET` → gradle이 `resValue`로 주입, 버튼 노출도 자동 결정(`build-apk.yml`).
    - 버튼 노출: `--dart-define=NAVER_LOGIN_ENABLED=true` 인 빌드에서만(`kNaverLoginConfigured`). 미주입 빌드는 버튼 숨김 + 정상 빌드.
    - 콘솔: developers.naver.com > API 설정 > 로그인 오픈 API 서비스 환경에 Android(패키지 `com.nulloongzi.nulloongzido`) / iOS(URL Scheme `nulloongzinaverlogin`, 번들 ID 동일) 추가 필요.
  - ⚠️ 네이버 SDK 구조상 secret이 앱 바이너리에는 포함된다(네이버 정책). 저장소 노출만 막는 조치임.
  - 카카오 콘솔: 로그인 활성화 + 동의항목 설정 필요(공유용 키·패키지 등록은 완료 상태).
  - 로그인 진행 안내: 제공자별 반투명 테마 레이어(카카오=번개·네이버=물결·구글=안개·이메일=습기, `auth_loading_layer.dart`) + 12초 지연 시 닫기 탈출구.
  - 네임카드 스탬프 이스터에그: 좌상단 밥이름 옆 로그인 수단 스탬프(`provider_stamp.dart`) — 웹과 동일 판별 규칙(uid 접두사 → providerData).

### 검증 필요(동작 미확정)
- 앱: 언어 전환 시 열려 있는 상세 패널 즉시 재렌더 여부
- 앱: 밥이름 생성 시 중복 재시도 유무

## 3. 이식 제외(플랫폼 특성)
- 웹: PWA 설치, OG/Twitter 메타, URL 주소창 동기화, html2canvas/다운로드 UX
- 앱: 네이티브 권한, App Links, 더블백 종료, 키보드 포커스 관리, BounceTap/글래스 모피즘 구현체
- 공용 백엔드(양쪽이 이미 공유): 카카오 챗봇 관리, verificationAction 승인 링크, adminReassignOwner·migrate Functions
